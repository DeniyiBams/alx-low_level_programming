#include "main.h"

/**
 * is_palindrome - function to check for palindrome
 * @s: string
 *
 * Return: 1 if palindrome, else 0
 */

int is_palindrome(char *s)
{
	if (*s == '\0')
		return (1);
	else
		return (0);
}
